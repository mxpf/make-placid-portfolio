# Keep false in distributable builds. Set true only in a private .env.local.
NEXT_PUBLIC_PORTFOLIO_CUSTOM_FONT=false
NEXT_PUBLIC_SHOW_PROJECT_LABELS=false

# Optional deployment overrides. Leave blank when hosting at the domain root.
NEXT_PUBLIC_BASE_PATH=
NEXT_PUBLIC_SITE_URL=
